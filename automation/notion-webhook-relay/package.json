{
  "name": "notion-webhook-relay",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "secret:github-token": "wrangler secret put GITHUB_DISPATCH_TOKEN",
    "secret:notion-token": "wrangler secret put NOTION_WEBHOOK_VERIFICATION_TOKEN"
  },
  "devDependencies": {
    "wrangler": "^4.13.2"
  }
}
